# python-sql-plotly-bar-chart
This Python script connects to a SQL Server database, retrieves data from a specific table by executing a SQL query, and visualises the data in a bar chart using Plotly.
